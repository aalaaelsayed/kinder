<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>view classes</title>
</head>
<body>

    <h1>{{ $classes->className }}</h1>
    <h5>{{ $classes->created_at  }}</h5>
    <h5>{{ $classes->updated_at  }}</h5>
    <p>{{ $classes->time  }}</p>
    <p>{{$classes->capacity}}</p>
    <p>{{ $classes->age  }}</p>
    <p>{{ $classes->price  }}</p>



</body>
</html>